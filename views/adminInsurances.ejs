<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>All Insurances</title>
    <style>
        @font-face {
            font-family: 'Matter-Regular';
            src: url('fonts/Matter-Regular.ttf') format('truetype');
            font-weight: normal;
            font-style: normal;
        }

        body {
            font-family: 'Matter-Regular', Arial, sans-serif;
            margin: 0;
            padding: 0;
            display: flex;
            background-color: #f8f9fa;
        }

        .dashboard {
            display: flex;
            width: 100%;
        }

        .sidebar {
            width: 250px;
            background-color: #F7FAFF;
            padding: 20px;
            display: flex;
            flex-direction: column;
        }

        .sidebar-logo {
            text-align: center;
            margin-bottom: 20px;
        }

        .sidebar-logo img {
            width: 170px;
            height: auto;
        }

        .sidebar-menu {
            list-style: none;
            padding: 0;
        }

        .sidebar-menu-item {
            margin: 10px 0;
        }

        .sidebar-menu-item a {
            text-decoration: none;
            color: #A0AAC8;
            display: block;
            padding: 13px;
            transition: color 0.3s ease;
        }

        .sidebar-menu-item a.active,
        .sidebar-menu-item a:hover {
            color: #42427D;
            background-color: transparent;
        }

        .sidebar h2 {
            color: #42427D;
            margin: 0 0 20px 0;
        }

        .main-content {
            flex: 1;
            padding: 20px;
            border: 1px solid #e9ecef;
            border-radius: 5px;
            background-color: #fff;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
            margin: 20px;
        }

        .container {
            margin: 0 auto;
            max-width: 1200px; 
        }

        .container h1 {
            margin-top: 0;
            text-align: center;
            color: #42427D;
        }

        .container {
            display: flex;
            flex-direction: column;
            align-items: center; 
        }

        .container a.add-new {
            margin-bottom: 20px;
            padding: 10px 15px;
            border: 1px solid #42427D;
            color: #42427D;
            text-decoration: none;
            border-radius: 5px;
            font-size: 14px;
            cursor: pointer;
            margin-top: 10px;
            transition: background-color 0.3s ease, color 0.3s ease;
            text-align: center;
            background-color: transparent; 
        }

        .container a.add-new:hover {
            background-color: #f1f1f1;
            color: #42427D;
        }

        ul {
            list-style: none;
            padding: 0;
            width: 100%; 
        }

        .insurance-item {
            border-bottom: 1px solid #a8afb7;
            padding: 10px 0;
        }

        .insurance-item h2 {
            margin-top: 0;
            color: #42427D;
        }

        .insurance-details {
            display: flex;
            align-items: center;
            justify-content: space-between;
        }

        button {
            background-color: transparent;
            color: #42427D;
            border: 1px solid #42427D;
            padding: 10px 15px;
            border-radius: 5px;
            cursor: pointer;
            font-size: 14px;
            margin-top: 10px;
            transition: background-color 0.3s ease, color 0.3s ease;
        }

        button:hover {
            background-color: #42427D;
            color: white;
        }

        .delete-button {
            color: #FF7F50;
            border: 1px solid #FF7F50;
        }

        .delete-button:hover {
            background-color: #FF7F50;
            color: white;
        }

        .edit-button {
            color: #007BFF;
            border: 1px solid #007BFF;
            text-decoration: none;
            padding: 10px 15px;
            border-radius: 5px;
            font-size: 14px;
            display: inline-block;
            margin-top: 10px;
            transition: background-color 0.3s ease, color 0.3s ease;
        }

        .edit-button:hover {
            background-color: #007BFF;
            color: white;
        }

        img {
            max-width: 150px;
            height: auto;
            display: block;
            margin-right: 20px;
        }
    </style>
</head>
<body>
    <div class="dashboard">
        <div class="sidebar">
            <div class="sidebar-logo">
                <img src="/logo.png" alt="Admin Logo">
            </div>
            <h2>Admin Functions</h2>
            <ul class="sidebar-menu">
                <li class="sidebar-menu-item"><a href="/admin/blogs" <% if (activePage === 'blogs') { %> class="active" <% } %>>View Admin Blogs</a></li>
                <li class="sidebar-menu-item"><a href="/admin/blog" <% if (activePage === 'blog-upload') { %> class="active" <% } %>>Upload Admin Blog</a></li>
                <li class="sidebar-menu-item"><a href="/admin/doctor-profile-requests" <% if (activePage === 'doctor-profile-requests') { %> class="active" <% } %>>Doctor Profile Verification Requests</a></li>
                <li class="sidebar-menu-item"><a href="/admin/subscriptions" <% if (activePage === 'subscriptions') { %> class="active" <% } %>>Doctor Subscription Verification Requests</a></li>
                <li class="sidebar-menu-item"><a href="/admin/insurances" <% if (activePage === 'insurances') { %> class="active" <% } %>>Manage Insurance Providers</a></li>
                <li class="sidebar-menu-item"><a href="/admin/view-appointments" <% if (activePage === 'view-appointments') { %> class="active" <% } %>>View Appointments</a></li>
                <li class="sidebar-menu-item"><a href="/admin/view-doctors" <% if (activePage === 'view-doctors') { %> class="active" <% } %>>View Doctors</a></li>
                <li class="sidebar-menu-item"><a href="/admin/view-patients" <% if (activePage === 'view-patients') { %> class="active" <% } %>>View Patients</a></li>
                <li class="sidebar-menu-item"><a href="/admin/manage-payments" <% if (activePage === 'manage-payments') { %> class="active" <% } %>>Manage Payments</a></li>
                <li class="sidebar-menu-item"><a href="/admin/specialty/new" class="<%= activePage === 'specialties' ? 'active' : '' %>">Manage Specialties</a></li>
                <li class="sidebar-menu-item"><a href="/admin/condition/new" class="<%= activePage === 'conditions' ? 'active' : '' %>">Manage Conditions</a></li>
                <li class="sidebar-menu-item"><a href="/admin/news-releases" <% if (activePage === 'news-releases') { %> class="active" <% } %>>Manage News Releases</a></li>
                <li class="sidebar-menu-item"><a href="/admin/news-logos" <% if (activePage === 'news-logos') { %> class="active" <% } %>>Manage News Logos</a></li> <!-- Added link for managing logos -->
                <li class="sidebar-menu-item"><a href="/auth/logout" <% if (activePage === 'logout') { %> class="active" <% } %>>Logout</a></li>
            </ul>
        </div>

        <div class="main-content">
            <div class="container">
                <h1>All Insurances</h1>
                <a href="/admin/insurance/new" class="add-new">Add New Insurance Provider</a>
                <ul>
                    <% insurances.forEach(insurance => { %>
                        <li class="insurance-item">
                            <h2><%= insurance.name %></h2>
                            <div class="insurance-details">
                                <% if (insurance.logo && insurance.logo.data && (insurance.logo.contentType === 'image/png' || insurance.logo.contentType === 'image/jpeg')) { %>
                                    <img src="data:<%= insurance.logo.contentType %>;base64,<%= insurance.logo.data.toString('base64') %>" alt="Insurance Logo" />
                                <% } else { %>
                                    <p>No valid logo available</p>
                                <% } %>
                                <a href="/admin/insurances/edit/<%= insurance._id %>" class="edit-button">Edit</a>
                                <form action="/admin/insurance/delete/<%= insurance._id %>" method="POST" onsubmit="return confirm('Are you sure you want to delete this insurance?');">
                                    <button type="submit" class="delete-button">Delete</button>
                                </form>
                            </div>
                        </li>
                    <% }) %>
                </ul>
            </div>
        </div>
    </div>
</body>
</html>
