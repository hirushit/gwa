<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>View Doctors</title>
  <style>
    /* Importing the custom font */
    @font-face {
      font-family: 'Matter-Regular';
      src: url('fonts/Matter-Regular.ttf') format('truetype');
      font-weight: normal;
      font-style: normal;
    }

    /* Basic Styles */
    body {
      font-family: 'Matter-Regular', Arial, sans-serif;
      margin: 0;
      padding: 0;
      display: flex;
      background-color: #f8f9fa;
    }

    .dashboard {
      display: flex;
      width: 100%;
    }

    .sidebar {
      width: 250px;
      background-color: #F7FAFF; /* Updated sidebar color */
      padding: 20px;
      display: flex;
      flex-direction: column;
      box-shadow: 2px 0 5px rgba(0,0,0,0.1);
    }

    .sidebar-logo {
      text-align: center; /* Center align logo */
      margin-bottom: 20px;
    }

    .sidebar-logo img {
      width: 170px;
      height: auto;
    }

    .sidebar-menu {
      list-style: none;
      padding: 0;
    }

    .sidebar-menu-item {
      margin: 10px 0;
    }

    .sidebar-menu-item a {
      text-decoration: none;
      color: #A0AAC8; /* Text color */
      display: block;
      padding: 10px;
      border-radius: 5px;
      transition: color 0.3s ease;
    }

    .sidebar-menu-item a.active,
    .sidebar-menu-item a:hover {
      color: #42427D; /* Text color on hover or active */
      background-color: transparent; /* No background color change */
    }

    .sidebar h2 {
      margin: 0 0 20px 0; /* Remove extra margin around heading */
      color: #42427D; /* Updated color for consistency */
    }

    .main-content {
      flex: 1;
      padding: 20px;
    }

    .container {
      width: 100%;
      margin: 20px auto;
      padding: 20px;
      border: 1px solid #e9ecef;
      border-radius: 5px;
      background-color: #fff;
      box-shadow: 0 2px 5px rgba(0,0,0,0.1);
    }

    h1 {
      margin-bottom: 20px;
      text-align: center;
      color: #42427D; /* Updated color for consistency */
    }

    ul {
      list-style: none;
      padding: 0;
    }

    .doctor-item {
      margin-bottom: 10px;
      padding: 10px;
      display: flex;
      justify-content: space-between;
      align-items: center;
      border-radius: 5px;
      border: 1px solid #e9ecef;
      display: flex;
      align-items: center;
    }

    .doctor-info {
      display: flex;
      flex-direction: column;
    }

    .doctor-info strong {
      font-size: 16px;
      color: #42427D; 
    }

    .doctor-info span {
      font-size: 14px;
      color: #42427D;
    }

     button {
      background-color: transparent;
      color: #FF7F50;
      border: 2px solid #FF7F50;
      padding: 5px 10px;
      border-radius: 5px;
      cursor: pointer;
      font-size: 14px;
      transition: background-color 0.3s ease, color 0.3s ease;
    }

    button:hover {
      background-color: #FF7F50;
      color: white;
    }

    a.edit {
      display: flex;
      background-color: transparent;
      color: #FF7F50;
      border: 2px solid #FF7F50;
      padding: 5px 10px;
      border-radius: 5px;
      cursor: pointer;
      font-size: 14px;
      justify-content: center;
      transition: background-color 0.3s ease, color 0.3s ease;
    }
    
    a:hover {
      background-color: #FF7F50;
      color: white;
    }
    
    .action-buttons {
      display: flex;
      gap: 10px;
    }
    input[type="text"], select {
      padding: 10px;
      margin-right: 10px;
      font-size: 14px;
      border: 1px solid #e9ecef;
      border-radius: 5px;
      background-color: #f8f9fa;
      color: #42427D; /* Text color for input and select */
      width: 200px; /* Adjust width as needed */
      transition: border-color 0.3s ease, background-color 0.3s ease;
    }
  
    input[type="text"]:focus, select:focus {
      border-color: #FF7F50;
      background-color: #ffffff;
      outline: none;
    }
  
    select {
      -webkit-appearance: none;
      -moz-appearance: none;
      appearance: none;
      padding-right: 30px; /* Space for dropdown arrow */
      background-image: url('data:image/svg+xml;base64,...'); /* Add an SVG or icon for the dropdown arrow */
      background-repeat: no-repeat;
      background-position: right 10px center;
    }
  
    /* Adjusting the size and placement of the dropdown arrow */
    select::-ms-expand {
      display: none; /* Remove the dropdown arrow in IE */
    }
  
    button[type="submit"] {
      background-color: #FF7F50;
      color: white;
      border: none;
      padding: 10px 20px;
      border-radius: 5px;
      cursor: pointer;
      font-size: 14px;
      transition: background-color 0.3s ease, color 0.3s ease;
    }
  
    button[type="submit"]:hover {
      background-color: #FF5733;
    }

  </style>
</head>
<body>
  <div class="dashboard">
    <div class="sidebar">
      <div class="sidebar-logo">
        <img src="/logo.png" alt="Admin Logo">
      </div>
      <h2>Admin Functions</h2>
      <ul class="sidebar-menu">
        <li class="sidebar-menu-item"><a href="/admin/blogs" <% if (activePage === 'blogs') { %> class="active" <% } %>>View Admin Blogs</a></li>
        <li class="sidebar-menu-item"><a href="/admin/blog" <% if (activePage === 'blog-upload') { %> class="active" <% } %>>Upload Admin Blog</a></li>
        <li class="sidebar-menu-item"><a href="/admin/doctor-profile-requests" <% if (activePage === 'doctor-profile-requests') { %> class="active" <% } %>>Doctor Profile Verification Requests</a></li>
        <li class="sidebar-menu-item"><a href="/admin/subscriptions" <% if (activePage === 'subscriptions') { %> class="active" <% } %>>Doctor Subscription Verification Requests</a></li>
        <li class="sidebar-menu-item"><a href="/admin/insurances" <% if (activePage === 'insurances') { %> class="active" <% } %>>Manage Insurance Providers</a></li>
        <li class="sidebar-menu-item"><a href="/admin/view-appointments" <% if (activePage === 'view-appointments') { %> class="active" <% } %>>View Appointments</a></li>
        <li class="sidebar-menu-item"><a href="/admin/view-doctors" <% if (activePage === 'view-doctors') { %> class="active" <% } %>>View Doctors</a></li>
        <li class="sidebar-menu-item"><a href="/admin/view-patients" <% if (activePage === 'view-patients') { %> class="active" <% } %>>View Patients</a></li>
        <li class="sidebar-menu-item"><a href="/admin/manage-payments" <% if (activePage === 'manage-payments') { %> class="active" <% } %>>Manage Payments</a></li>
        <li class="sidebar-menu-item"><a href="/admin/specialty/new" class="<%= activePage === 'specialties' ? 'active' : '' %>">Manage Specialties</a></li>
        <li class="sidebar-menu-item"><a href="/admin/condition/new" class="<%= activePage === 'conditions' ? 'active' : '' %>">Manage Conditions</a></li>
        <li class="sidebar-menu-item"><a href="/admin/news-releases" <% if (activePage === 'news-releases') { %> class="active" <% } %>>Manage News Releases</a></li>
        <li class="sidebar-menu-item"><a href="/admin/news-logos" <% if (activePage === 'news-logos') { %> class="active" <% } %>>Manage News Logos</a></li> <!-- Added link for managing logos -->
        <li class="sidebar-menu-item"><a href="/auth/logout" <% if (activePage === 'logout') { %> class="active" <% } %>>Logout</a></li>
</ul>
    </div>

    <div class="main-content">
      <div class="container">
        <h1>Doctors</h1>
        <form action="/admin/manage-payments" method="GET">
          <input type="text" name="name" placeholder="Search by name" value="<%= name %>">
          <input type="text" name="email" placeholder="Search by email" value="<%= email %>">
          
          <select name="subscriptionType">
            <option value="">All Types</option>
            <option value="Standard" <%= subscriptionType === 'Standard' ? 'selected' : '' %>>Standard</option>
            <option value="Premium" <%= subscriptionType === 'Premium' ? 'selected' : '' %>>Premium</option>
            <option value="Enterprise" <%= subscriptionType === 'Enterprise' ? 'selected' : '' %>>Enterprise</option>
          </select>
        
          <button type="submit">Search</button>
        </form>
        

        <% if (doctors.length > 0) { %>
          <ul>
            <% doctors.forEach(doctor => { %>
              <li class="doctor-item">
                <div class="doctor-info">
                  <strong><%= doctor.name %></strong>
                  <span><%= doctor.email %></span>
                </div>
                <div class = "action-buttons">
                  <strong><%= doctor.subscriptionType %></strong>
                  <a class="edit" href="/admin/update-payments/<%= doctor._id %>">Pay the Settlement</a>

                </div>

              </li>
            <% }) %>
          </ul>
        <% } else { %>
          <p>No doctors found.</p>
        <% } %>

      </div>
    </div>
    
  </div>
</body>
</html>
