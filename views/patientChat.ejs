<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Patient Chat</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f0f0f0;
            margin: 0;
            padding: 0;
        }
        .container {
            max-width: 800px;
            margin: 20px auto;
            background-color: #fff;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0,0,0,0.1);
        }
        h1 {
            font-size: 24px;
            text-align: center;
        }
        .chat-box {
            border: 1px solid #ccc;
            border-radius: 8px;
            padding: 10px;
            height: 300px;
            overflow-y: scroll;
            margin-bottom: 10px;
        }
        .messages {
            list-style-type: none;
            padding: 0;
            margin: 0;
        }
        .patient-message {
            background-color: #e6f2ff; 
            padding: 8px 12px;
            border-radius: 8px;
            margin-bottom: 10px;
            max-width: 70%;
            word-wrap: break-word;
            box-shadow: 0 0 5px rgba(0, 0, 0, 0.1);
            align-self: flex-end; 
        }
        .doctor-message {
            background-color: #f2f2f2; 
            padding: 8px 12px;
            border-radius: 8px;
            margin-bottom: 10px;
            max-width: 70%;
            word-wrap: break-word;
            box-shadow: 0 0 5px rgba(0, 0, 0, 0.1);
            align-self: flex-start; 
        }
        .timestamp {
            font-size: 12px;
            color: #666;
            margin-left: 10px;
            display: block;
            text-align: right; 
        }
        #messageForm {
            display: flex;
            margin-top: 10px;
        }
        #messageForm input[type="text"] {
            flex: 1;
            padding: 10px;
            border: 1px solid #ccc;
            border-radius: 4px;
            font-size: 14px;
        }
        #messageForm button {
            padding: 10px 20px;
            border: none;
            background-color: #4CAF50;
            color: white;
            border-radius: 4px;
            cursor: pointer;
            font-size: 14px;
            margin-left: 10px;
        }
        #messageForm button:hover {
            background-color: #45a049;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Patient Chat</h1>
        <div class="chat-box">
            <ul class="messages">
                <% chat.messages.forEach(message => { %>
                    <li class="<%= message.senderId.toString() === chat.patientId.toString() ? 'patient-message' : 'doctor-message' %>">
                        <%= message.text %>
                        <span class="timestamp"><%= new Date(message.timestamp).toLocaleString() %></span>
                    </li>
                <% }); %>
            </ul>
        </div>
        <form id="messageForm" action="/patient/chats/<%= chat._id %>/send-message" method="POST">
            <input type="text" name="message" placeholder="Type your message here..." required>
            <button type="submit">Send</button>
        </form>
    </div>
</body>
</html>
