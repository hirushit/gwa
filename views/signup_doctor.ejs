<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Doctor Signup</title>
  <style>
    .google-signup-btn {
      display: inline-block;
      background-color: #4285F4;
      color: white;
      text-decoration: none;
      padding: 12px 24px;
      border-radius: 20px;
      transition: background-color 0.3s ease;
      position: relative;
      overflow: hidden;
      margin-top: 10px; 
    }
    .google-signup-btn:hover {
      background-color: #3171e0;
    }
    .google-signup-btn::before {
      content: "";
      position: absolute;
      left: 10px;
      top: 50%;
      transform: translateY(-50%);
      border-radius: 20px;
      width: 30px;
      height: 30px;
      background: url('https://developers.google.com/identity/images/g-logo.png') no-repeat;
      background-size: contain;
    }
    .google-signup-btn span {
      margin-left: 40px;
      vertical-align: middle;
      font-size: 16px;
    }
  </style>
</head>
<body>
  <h1>Doctor Signup</h1>
  
  <% if (success_msg) { %>
    <p style="color: green;"><%= success_msg %></p>
  <% } %>
  
  <% if (error_msg) { %>
    <p style="color: red;"><%= error_msg %></p>
  <% } %>

  <form action="/auth/signup/doctor" method="POST">
    <div>
      <label for="name">Name:</label>
      <input type="text" id="name" name="name" required>
    </div>
    <div>
      <label for="email">Email:</label>
      <input type="email" id="email" name="email" required>
    </div>
    <div>
      <label for="phoneNumber">Mobile Number:</label>
      <input type="text" id="phoneNumber" name="phoneNumber" required>
    </div>
    <div>
      <label for="password">Password:</label>
      <input type="password" id="password" name="password" required>
    </div>
    <button type="submit">Signup</button>
  </form>
  
  <br>
  <a href="/auth/google/doctor" class="google-signup-btn">
    <span>Signup with Google</span>
  </a>
  
  <br>
  <a href="/auth/signup/patient">Patient Signup</a>
  <br><br>
  <a href="/auth/login">Login</a>
</body>
</html>
